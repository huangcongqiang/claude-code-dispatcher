interface:
  display_name: "Claude Code Dispatcher"
  short_description: "派发 Claude Code 单任务或连续队列并复核结果"
  default_prompt: "Use $claude-code-dispatcher to delegate this task or task queue to Claude Code, review each result, and continue only after verification passes."
policy:
  allow_implicit_invocation: true
